import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class LoginPage extends Component {
  render() {
    return (
      <View>
        <Text> Sign in ekranı </Text>
      </View>
    )
  }
}

const styles = StyleSheet.create({})
